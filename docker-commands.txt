# Im Projektordner (dort wo dein Dockerfile liegt)
docker buildx build --platform linux/amd64 -t cloudflare-downloader:latest .

# Prüfen, ob Image vorhanden:
docker images cloudflare-downloader

# Als TAR exportieren:
docker save -o cloudflare-downloader.tar cloudflare-downloader:latest

#compact everything command
docker buildx build --platform linux/amd64 -t cloudflare-downloader:latest . && docker save -o cloudflare-downloader.tar cloudflare-downloader:latest